# PERF: dashboard observations, status/log transport, origin memory, origin outbox

Date: 2026-09-10. Foundation HEAD: `9297256f5ee3584ab8772dd0cac54304af60843e`.

## Goal

Foundation 5.3b / 5.4 / 5.5 is source-approved at `9297256`. This increment implements durable host origin delivery (outbox). Spec: `docs/superpowers/specs/2026-09-10-origin-outbox.md`.

## Architecture

Reuse `DashboardService` completed snapshot + single-flight collect + deadlines + 77bc222 facts TTL tracker. One collector thread is started with `DashboardHttpServer::bind` and stopped without waiting out a slow collect. HTTP `GET /api/v1/snapshot` reads the last snapshot (`read_snapshot`) and never collects. Last good is HTTP 200 with original `generated_at_millis`; failed collection sets `collection.freshness=stale`. No last good: 503 `DASHBOARD_SNAPSHOT_PENDING`. No new `age_millis` field (`model.rs` is FLOW). Three independent commits.

Reuse `HostOperation` + clap host commands. New `status-logs` is optional: try, then sequential fallback only for a helper that already matches `PROTOCOL_VERSION` but lacks the clap command. PERF does not bump `src/protocol.rs`; FLOW owns the dedicated v7 commit. Combined response is job `StatusResponse` + log chunks, never `TaskStatus`. Typed result checks stay typed (no log scraping). v6 peers mismatch at probe. Spec: `docs/superpowers/specs/2026-09-10-status-logs-wire.md`.

Origin source: laptop still `preflight_origin`; runner skips `push_base`; worker `fetch_origin` then pins `refs/mac-worker/bases/{task_id}` and `verify_base`. Streaming: drain ProcessRunner pipes after cap; prefer `last.md`; never return truncated protocol as success. Auth signature detection (external auth-incident, not cherry-picked) must compose with a rolling matcher over bounded windows, not a 256 MiB stderr allocation.

## Constraints and interfaces

Approved snapshot/status-log/origin interfaces live in this plan plus `docs/superpowers/specs/2026-09-10-status-logs-wire.md`. Root protocol decision: `/private/tmp/mac-worker-roadmap-jn00iibh/protocol-decision.md`. FLOW owns mutation routes/UI and the v7 version commit. ENV owns Git identity. External reconcile-repair owns `reconcile_runners` / early-exit diagnostic. External auth-incident owns adapter `auth_failure_signatures`. Cargo only through `cargo-slots.py`; focused suites; private target-dir.

## SSH multiplexing

Inspect effective `ssh -G` for ControlMaster/ControlPersist/ControlPath. Do not write user SSH config. If multiplexing is already effective, do not invent another layer.

## Verification

Focused tests listed in the contract. `cargo fmt --check`, focused tests, clippy `-D warnings` on the final SHA via the slot wrapper. No full all-targets / lib suite here.
